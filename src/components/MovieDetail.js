import React from "react";
import movies from "../movies";

const MovieDetail = ({ movie }) => {
  <h1>MOVIEEE</h1>;
  //const movieInfo = movies.find((movie) => movie.id);

  return <p>{movieInfo}</p>;
};

export default MovieDetail;
